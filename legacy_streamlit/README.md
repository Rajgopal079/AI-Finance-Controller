# FINCTRL AI — Legacy Streamlit UI

This directory contains the original Streamlit UI interface for historical reference.

The primary entry point for FINCTRL AI is now the React + TypeScript + Vite frontend (`frontend/`) powered by the FastAPI API layer (`backend/`).

To run the legacy Streamlit UI if needed:
```bash
streamlit run legacy_streamlit/main.py
```
